import 'package:flutter/material.dart';

getHeight({required BuildContext context}) =>
    MediaQuery.of(context).size.height;
getWidth({required BuildContext context}) => MediaQuery.of(context).size.width;
